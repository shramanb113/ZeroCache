# Data Retention Policy

Deleted files are moved to a recovery bin and permanently erased after 14 days. Files can be restored from the recovery bin at any time within that window via the dashboard or the API's `restore` endpoint. Account deletion permanently erases all buckets and their contents within 30 days, with no recovery option after that window closes.
